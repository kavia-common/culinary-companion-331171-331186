# Public (exposed to browser) configuration for the frontend.
# Set these in the deployment environment (.env is managed by orchestration).

# Base URL for the backend API (FastAPI). Example: https://example.com or http://localhost:3001
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# Optional: default page size for recipe search
NEXT_PUBLIC_DEFAULT_PAGE_SIZE=12
