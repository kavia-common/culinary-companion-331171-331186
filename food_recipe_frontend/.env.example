# Frontend -> Backend wiring
# Set this to the backend origin (no trailing slash preferred).
# Example (local FastAPI): http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=

# Optional: browse page default page size
NEXT_PUBLIC_DEFAULT_PAGE_SIZE=12
